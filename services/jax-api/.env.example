# Environment variables for running jax-api locally (do NOT commit secrets)
# Example Postgres DSN for Docker Compose setup
# If set, this overrides the `postgresDsn` value in `config/jax-core.json`.
DATABASE_URL=postgresql://jaxuser:jaxpass@postgres:5432/jaxdb

# API config
# PORT=8081
