# Upstream

- Source (likely): https://github.com/aiming-lab/Agent0.git
- Vendored into this monorepo (no nested git repo).

Note: this upstream repo contains multiple projects (e.g. Agent0 and Agent0-VL).

