# Upstream

- Source (likely): <https://github.com/virattt/dexter.git>
- Vendored into this monorepo (no nested git repo).

If you need to re-sync with upstream later, we can switch to a `git subtree` workflow and record a pinned commit at that point.

