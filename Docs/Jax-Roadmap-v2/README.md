# Jax Roadmap v2 — Historical Supporting Pack

This directory is a historical/supporting implementation pack. It is not a current roadmap authority and its `NEXT-WORK-PACKAGE.md` is a compatibility pointer only.

Current truth is in `Docs/ROADMAP.md`, `Docs/STATUS.md`, and `Docs/BUILD/CURRENT_PACKAGE.md`. Historical phase briefs, gates, evidence navigation, and the append-only decision log are retained for provenance. Do not infer current authorization from this pack.

`MANIFEST.sha256` fingerprints the pack files except the manifest itself.
