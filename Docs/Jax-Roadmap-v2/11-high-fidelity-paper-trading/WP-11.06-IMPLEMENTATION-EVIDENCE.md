# WP-11.06 implementation evidence

Status: `IMPLEMENTED / INTERNALLY VERIFIED`

`Reconcile` compares paper venue orders and fills with a reconstructed
event-derived paper ledger. It validates immutable identities, fill-to-order
references, aggregate quantities, order states, ledger cash/positions/fees/
realised P&L and explicit market references. Missing or corrupt orders/fills,
quantity mismatches, invalid environment, stale market data and ledger
divergence produce deterministic `RECONCILIATION_REQUIRED` results with stable
reason codes. Valid frozen state produces `CLEAN`.

Verification:

- `go test ./internal/modules/papertrading -count=1` — PASS.
- Covered clean reconciliation, corrupted ledger state, stale public-market
  reference, order/fill linkage and quantity/state consistency.

Reconciliation never advances workflow state, calls a broker or writes the
Phase-09 observed portfolio.
