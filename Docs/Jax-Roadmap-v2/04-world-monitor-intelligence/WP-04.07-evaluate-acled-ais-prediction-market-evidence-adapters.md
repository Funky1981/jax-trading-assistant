# Work Package 04.07 — Evaluate ACLED/AIS/prediction-market evidence adapters

**Status:** **IMPLEMENTED / INTERNALLY VERIFIED**

## Objective
Deliver **Evaluate ACLED/AIS/prediction-market evidence adapters** as the next smallest independently reviewable increment of Phase 04.

## Before coding
1. Read this phase `README.md`, `GATE.md`, and governance rules.
2. Inspect existing Jax code/contracts first. Prefer evolution over duplicate subsystems.
3. Inspect the relevant Fincept/reference implementation at concept level and record what is current, target-only, dormant or externally delegated.
4. Check mature external libraries/services before implementing commodity functionality from scratch.
5. Record the chosen approach and rejected alternatives when the choice is architectural.

## Required implementation characteristics
- Keep the package bounded and reversible where practical.
- Prefer explicit typed/versioned contracts between runtimes.
- Do not introduce a new runtime/language unless the capability boundary justifies its operational cost.
- Preserve deterministic validation around AI outputs.
- Add observability appropriate to the failure modes introduced.

## Acceptance criteria
- The deliverable for **Evaluate ACLED/AIS/prediction-market evidence adapters** is implemented only within the scope of Phase 04.
- Success and material failure/negative paths have automated or reproducible verification.
- Raw/provider representation and canonical/normalized representation remain distinguishable; freshness/quality state is explicit.
- Any new persisted or derived data carries the phase-required provenance/version identity.
- Existing safety and paper/live invariants are explicitly verified.
- Operator-visible errors/failures do not masquerade as success.
- Codex returns the standard review handover and stops.

## Required handover
Use `../governance/CODEX-REVIEW-HANDOVER.md`.

**STOP after the handover. Do not begin the next work package.**

## Internal implementation evidence

- Versioned, read-only adapter evaluations now cover ACLED structured conflict
  evidence, AIS maritime corroboration, and public prediction-market signals.
- ACLED is explicitly deferred for external credential/licensing provisioning;
  AIS is marked beta/no-SLA and corroboration-only; prediction markets are
  marked participant signals rather than event truth. These limitations flow
  into the assembled result's unknowns.
- Adapter evidence requires an adapter identity, external identity, schema
  version, and exact non-empty raw bytes. No adapter has a write, order,
  approval, or execution capability.
- The full deterministic `Analyze` pipeline and exit proof are covered by
  `TestPhase04ExitConditionReplayedMultiSourceEvent`.
- Verification: `go test ./internal/modules/worldmonitorintelligence -count=1`.
- Adversarial review found and fixed missing propagation of the prediction
  market's non-truth limitation into final unknowns; duplicate adapter IDs,
  writable specs, missing raw bytes, and credential/beta paths are rejected or
  made explicit.
