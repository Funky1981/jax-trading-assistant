# WP-10.02 implementation evidence

Status: `IMPLEMENTED / INTERNALLY VERIFIED`

`Confirmation` is a durable, content-addressed review artifact containing the
workflow, recommendation, Phase-09 risk decision, portfolio snapshot, policy,
proposal, instrument, direction, resulting descriptive value, actor, decision,
UTC confirmation time and deterministic expiry. `WithDecision` is required to
bind the human actor and explicit approve/reject decision.

Approval never follows silence, timeout, model output or retry. The store
accepts only `ActorHuman` with an exact matching actor identity, only while the
workflow is awaiting confirmation, and only when the confirmation content and
expiry validate. Changed facts, wrong workflows, expired confirmations and
missing decisions fail closed.

Verification:

- `go test ./internal/modules/workflow -count=1` — PASS.
- Covered: explicit approval, researcher self-approval denial, changed-fact
  denial, expiry denial and paper-only safety flags.
