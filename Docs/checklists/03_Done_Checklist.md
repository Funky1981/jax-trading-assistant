# Checklist — Step 03 Done

- [ ] `MemoryStore` interface exists
- [ ] `InMemoryMemoryStore` fake exists with unit tests
- [ ] `HindsightClient` adapter exists with httptest-based unit tests
- [ ] `jax-memory` service compiles
- [ ] Integration test (tagged) can Ping + Retain + Recall
