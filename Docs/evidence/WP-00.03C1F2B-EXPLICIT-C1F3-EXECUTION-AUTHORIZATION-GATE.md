# WP-00.03C1F2B — Explicit C1F3 Execution-Authorization Gate

Status: offline control-plane package complete; C1F3 inference not executed. Phase 00 remains NO-GO.

## Authorization mechanism

- Identity: `ai-shadow-c1f3-execution-authorization-v1`
- Authorization fingerprint: `bcb453f3ed5be1d7066b8b5a2f0595354ba2406f943bb6e080fe90f7dd24450a`
- Control-plane source-set SHA-256: `3a5f9560c3dbe5fd04e955cb3837e374ffb8d7d176eed8cbe040d28715b3e9c1`
- Operator opt-in: process-local CLI flag `--authorize-c1f3-execution`; default false; valid only with `--execute`.
- Global gate: provider construction independently requires `JAX_AI_HOSTED_INFERENCE_AUTHORIZED=true`.
- Credential: future execution requires presence-only validation of `JAX_OPENAI_EXPERIMENT_API_KEY`; this package did not load it.
- Default: deny before provider construction. Either authorization alone is insufficient.
- Scope: only `openai-hosted-c1f3-generalization-v3` (48 cases, one repetition) and `openai-hosted-c1f3-boundary-v3` (32 cases, one repetition), both with provider `openai` and model `gpt-5.6-luna`.
- Registry boundary: the accepted C1F2A profiles remain `executable=false` and absent from the historical hosted registry. The two profiles are available only through the explicit C1F3 control-plane execution-profile loader.
- Revalidation: the exact registered profile, frozen bindings, execution shape, budget, collision state, provider-input isolation, two authorization keys, and credential presence are recomputed immediately before provider construction.

The C1F3 opt-in cannot authorize C1E3, original, v1, v2, another provider/model/dataset/profile/experiment/case count/repetition, or caller-supplied hash overrides. The C1E3 opt-in cannot authorize C1F3.

## Zero-network preflight

Credentialless planning remains available only while hosted authorization and C1F3 opt-in are false. It validates the exact dataset/input-lock/freeze, typed sidecar, adjudication rubric, scoring rubric, prompt v6, output v5 schema, C1F validator, C1E policy, semantic comparator, resolver, execution shape, namespace collision state, budget, runtime safety, and provider-visible input isolation.

It does not load an API key, construct a provider, contact a provider, or execute inference. The generated plan records:

- `provider_contact=false`
- `inference=false`
- `execution_authorized=false`
- `credential_loaded=false`
- `hosted_inference_authorized=false`
- `database_mutation=false`
- `trading_mutation=false`

## Conservative offline cost calculation

Pricing inputs supplied by the technical lead were used without provider contact: input USD 0.20/M, cached input USD 0.02/M, cache writes USD 0.25/M, and output USD 1.20/M. The calculator assumes every case consumes its full 256-token output cap and also requires one conservative corrective request.

| Frozen cell | Hard ceiling | Conservative maximum |
| --- | ---: | ---: |
| Generalization v3, 48 × 1 | USD 0.30 | USD 0.186754 |
| Boundary v3, 32 × 1 | USD 0.20 | USD 0.124656 |
| Combined | USD 0.50 | USD 0.311410 |

These are offline upper estimates, not execution approval or spend targets. The future C1F3 execution package must independently reverify live pricing, Responses API support, Structured Outputs support, and reasoning effort `none` immediately before inference.

## Mock construction and answer isolation

The command-level matrix injects a mock OpenAI provider constructor. Default, one-key, wrong-scope, wrong-binding, missing-credential, and colliding-namespace states make zero constructor calls. Each exact fully authorized C1F3 cell reaches the injected constructor exactly once; the mock returns `nil`, so no HTTP client is created or invoked and execution stops before any request.

The newly enabled planning path constructs and inspects every provider-visible initial request. It fails closed if expected mapping, issuer, proxy, typed-role, rationale, adjudication, or scoring field names are present. Sidecars and rubrics are loaded only for frozen validation and are never attached to `PreparedDiagnostic` or provider requests.

## Offline evidence

- Path: `.runtime/ai-shadow/offline-c1f2b-c1f3-execution-authorization-v1/WP-00.03C1F2B/offline-preflight.json`
- SHA-256: `42833f7967f77fe799fabc403e905d7f23e1988084ed970b1d59b8e83b50860d`
- Authorization fingerprint: `bcb453f3ed5be1d7066b8b5a2f0595354ba2406f943bb6e080fe90f7dd24450a`
- Control-plane source-set SHA-256: `3a5f9560c3dbe5fd04e955cb3837e374ffb8d7d176eed8cbe040d28715b3e9c1`

The artifact binds both exact profile fingerprints and all frozen semantic/file hashes. It records default deny, both-key construction, zero-network planning, unchanged frozen inputs, no provider construction/contact, no inference, no credential load, and no database/trading mutation.

## Safety disposition

- No C1F3 Generalization or Boundary execution occurred.
- No Luna, Terra, Sol, DeepSeek, Ministral, or Ollama inference occurred as a Jax workload.
- No real provider request or paid API call occurred.
- No model output or v3 score artifact was created.
- No dataset, sidecar, rubric, scorer, prompt, schema, validator, policy, comparator, or resolver semantic source changed.
- No database, trading, order, fill, approval, ticket, or broker mutation occurred.
- C1F3 execution opt-in and hosted inference authorization remain false/unset outside injected test state.

C1F2B stops here. It does not authorize execution in this package, repeatability work, WP-00.04, Phase 01, or a Phase 00 GO decision.
