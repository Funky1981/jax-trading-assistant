# WP-00.03C1F3B — Generalization repeatability cell freeze and authorization

Status: offline repeatability freeze and control plane complete; repeatability inference not executed. Phase 00 remains NO-GO pending the separately authorized repeatability execution and scoring result.

## Scope and invariant

C1F3B creates exactly one future provider cell: Generalization v3, 48 cases, one additional repetition. It does not create or authorize a Boundary repeat, a new holdout, remediation, tuning, semantic changes, database/trading mutation, or broker activity.

The provider-visible request is byte-for-byte equal to the accepted original C1F3 Generalization request for every frozen event. The new profile, original answers, baseline mappings, comparison metadata, v3 typed labels, scoring values, and failure IDs remain control-plane/scorer-only.

## Frozen identities

| Item | Identity | Hash/fingerprint |
| --- | --- | --- |
| Repeatability profile | `openai-hosted-c1f3-repeatability-generalization-v3-r2` | `294433030c9e24ff695b0a6792e7039555de21cdcfce8cc86805a3c86d45964d` |
| Comparison scoring freeze | `ai-shadow-c1f3-repeatability-scoring-v1` | semantic fingerprint `70313e7f77f5f82b912c382d96aa513f8e67dc03f53abe09c26189494677d1f5`; file SHA-256 `ff6fe9618ad8e89440e4d015724a9949c95f0542e6b0e0048bcb5d73f7e4c45d` |
| Comparison implementation | `ai-shadow-c1f3-repeatability-scoring-v1` | source SHA-256 `7b5e36b96bda8456b20841c59ec759ddf8f31472b288668b6880c4e7deb73114` |
| Reused accuracy scorer | `ai-shadow-causal-attribution-scoring-c1f3-v1` | source SHA-256 `f54ec91af32a9a11f3c0a7f27eefd3c52624d2360fce613820e7aa39380d8269` |
| Execution authorization | `ai-shadow-c1f3-repeatability-execution-authorization-v1` | `c0a389adcc6df98f2d8655686e9275fa323752614eed929e11b5f59d2810d9d9` |

The repeatability profile binds OpenAI, `gpt-5.6-luna`, reasoning effort `none`, prompt v6, output contract v5, the C1F validator, C1E policy, semantic identity v1, resolver v1, the unchanged C1F3 scorer, Generalization v3 dataset/input lock/freeze, the v3 typed sidecar and fingerprints, the C1F2A adjudication rubric, 48 cases, and exactly one repetition.

## Immutable baseline and accepted evidence

The comparison baseline is permanently bound to original Generalization run `0a650e09-1c64-4349-bf5d-09bf4dd697d9` and artifact-index SHA-256 `681db26c0d3e83e85537671e519db55c2b77397b03feb0c8a9395dd87b0ea0cb`.

Preflight also preserves and verifies:

- original Boundary run `9da5becc-41de-4946-8703-b1c9b620382e`, artifact-index SHA-256 `d551d69ced918432fa5e3fa0fa78e99c6009c8bf3030358a1cf8a8adbca0260d`;
- C1F3A identity `ai-shadow-c1f3-reprojection-v1`, fingerprint `8f860c0a8abcdc755b737b59c6feeb17af796a00c97afc900443a663563eba05`;
- C1F3A reprojection JSON SHA-256 `3d1990d1a0da5b6c2890fad2231b06394a5b0a3f55e43a545a4f93155506db70`;
- C1F3A artifact-index SHA-256 `73396d5f1bd77106d256d2d2639f6e6e8006521dd0070a5eee36c0a082d5b9c1`.

No original or derived accepted evidence was modified.

## Frozen comparison and gates

The primary metric is case-for-case semantic effective-mapping agreement. DIRECT requires the same class and an `EXACT` or `EQUIVALENT` issuer comparison under `ai-shadow-issuer-semantic-identity-v1`; PROXY requires the same class and case-normalized bounded exposure; UNRESOLVED requires the same class. The hard primary gate is:

`semantic effective-mapping agreement numerator / 48 / percentage >= 90%`

The repeat run must independently retain final validity at least 98%, semantic DIRECT precision at least 95%, semantic DIRECT recall at least 90%, semantic false-DIRECT at most 5% of 48, zero incorrect deterministic ticker/rule resolutions, and zero safety/persistence violations.

Strict mapping, whole typed attribution, every named causal role, principal-proxy candidates, attribution completeness, policy decisions, and deterministic resolutions are reported on denominator 48 without new hard thresholds. Every semantic mapping difference records both mappings, issuer/proxy values, identity outcome, typed-role differences, both policy and resolver projections, and whether the case is both correct, original wrong/repeat correct, original correct/repeat wrong, both wrong the same way, or both wrong differently. Original failures 041, 043, and 048 remain evidence only.

## Authorization and zero-network preflight

Provider construction requires both process-local `--authorize-c1f3-repeatability` and `JAX_AI_HOSTED_INFERENCE_AUTHORIZED=true`. Either alone denies. The authorization is valid only for the exact registered repeatability profile, baseline, artifact index, semantic hashes, model, 48 cases, one repetition, `$0.30` maximum ceiling, and collision-free new namespace. Original C1F3 authorization cannot authorize repeatability, and repeatability authorization cannot authorize original C1F3.

Credentialless preflight run:

- run ID: `f52cbccd-22ea-4c1f-9e9b-385c99190d17`
- artifact: `.runtime/ai-shadow/c1f3b-preflight-freeze/openai-hosted-c1f3-repeatability-generalization-v3-r2/WP-00.03C1F3B-GENERALIZATION-R2/preflight/f52cbccd-22ea-4c1f-9e9b-385c99190d17/preflight.json`
- SHA-256: `4908a354d7215a62f2a227f0bcae05feeae852075478d4ab6e254dabf5283939`
- `provider_contact=false`, `inference=false`, `execution_authorized=false`, `api_key_present=false`
- `frozen_bindings_valid=true`, `baseline_binding_valid=true`, `repeatability_scoring_valid=true`
- `evidence_namespace_collision_free=true`, `provider_input_isolated=true`, `provider_input_matches_original_c1f3=true`, `runtime_safety_valid=true`
- conservative maximum future cost: `$0.186754`; hard ceiling: `$0.30`

The preflight child process explicitly removed `JAX_OPENAI_EXPERIMENT_API_KEY` without reading it and set hosted inference authorization to false. The parent environment remained unset. Provider requests: 0. Model inference: 0. Cost: `$0`.

## Mock construction and validation

Command-level tests inject a nil provider constructor, while package-level tests tamper each frozen binding before the same construction gate. Default deny, either single authorization, wrong profile/baseline/index/model/count/repetition/hash, missing credential, and colliding namespace all deny before real construction. The exact frozen dual-authorized combination reaches the injected constructor once, returns nil, and stops with `provider is required`; no HTTP client or request is created. The zero-network preflight makes zero constructor calls.

Validation completed:

- repeatability profile, scoring, authorization, baseline-binding, provider-isolation, namespace-collision, mock-construction, cross-authorization, changed-case, and stable-but-degraded tests;
- `go test -count=1 ./internal/modules/aishadow`;
- `go test -count=1 ./cmd/ai-shadow-issuer-diagnostic`;
- `go test -count=1 ./...`;
- `go vet ./...`;
- `scripts/golden-check.ps1 -Mode verify`;
- `go test -count=1 ./tests/replay/...`;
- touched-file `gofmt` verification and `git diff --check`;
- production credential scan and explicit accepted-evidence/semantic-source hash checks.

`scripts/go-verify.ps1 -Mode full` stops at its repository-wide formatting check because of pre-existing unrelated formatting debt. No touched file appears in that list, and this package does not modify the unrelated files.

C1F3B stops here. The repeatability cell was not executed, Luna was not called, no semantic remediation was performed, and WP-00.04, Phase 01, and push remain out of scope.
