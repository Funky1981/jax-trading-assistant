# 02 — Safe Workflows

Implement after core Jax APIs are stable:

```text
nightly ETF data backfill
morning ETF news digest
provider health alert
candidate trade notification
end-of-day paper trade summary
post-trade reflection batch
```

n8n calls Jax. Jax validates/persists.
