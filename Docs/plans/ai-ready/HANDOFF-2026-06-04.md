# AI-ready completion audit and handoff - 2026-06-04

## Summary

`Docs/plans/ai-ready` is not complete. The implemented and documented work reaches ticket 09. Tickets 10-20 remain open, with a few placeholder or preparatory pieces already present in the frontend and backend contracts.

The folder should stay in `Docs/plans/ai-ready` until the remaining sentiment feature layer, API contracts, evidence UX, notification rules, research integration, channel preferences, reporting, and provider abstraction are completed.

## Committed docs work before this audit

- `093bd01 docs: rename automation n8n plan folder`

## Evidence reviewed

- `Docs/plans/ai-ready/README.md`
- `Docs/plans/ai-ready/handoff-00-02-summary.md`
- Ticket specs `Docs/plans/ai-ready/00-*.md` through `Docs/plans/ai-ready/20-*.md`
- `frontend/src/data/types.ts`
- `frontend/src/data/opportunity-adapter.ts`
- `frontend/src/pages/AiTradingPage.tsx`
- `frontend/src/pages/ApprovalsPage.tsx`
- `frontend/src/pages/ResearchPage.tsx`
- `frontend/src/pages/NotificationCentrePage.tsx`
- `frontend/src/data/notification-adapter.ts`
- `cmd/trader/ai_overview_handlers.go`
- `cmd/trader/event_classification.go`
- `cmd/research/backtest_strategytypes.go`
- `db/postgres/migrations`

## Completed tickets

| Ticket | Status | Evidence |
| --- | --- | --- |
| 00 Design system foundation | Complete | `handoff-00-02-summary.md` records completion; committed code includes design tokens/primitives. |
| 01 Homepage simplified nav | Complete | `HomePage` and simplified route/nav work are present and recorded in handoff. |
| 02 AI trading opportunity feed | Complete | `AiTradingPage` and unified feed shell are present and recorded in handoff. |
| 03 Opportunity adapter | Complete | `frontend/src/data/opportunity-adapter.ts` maps signals, candidates, and approvals into `OpportunitySummary`. |
| 04 Scanner sentiment controls | Complete for the ticket scope | `ScannerSettingsCard` and `AIScannerApiState.sentiment` settings are wired. |
| 05 Manual ETF policy reroute | Complete | `OrderTicketPanel` reroute behavior and tests were committed in the Phase 1 continuation. |
| 06 Notification centre inbox | Complete for in-app v1 | `NotificationCentrePage` and `notification-adapter` are present. |
| 07 Research wizard v1 | Complete for v1 | `ResearchPage` has guided wizard flow and tests. |
| 08 Baseline analytics events | Complete | `frontend/src/lib/analytics.ts` and event hooks are present. |
| 09 AI overview and scanner API | Complete | `cmd/trader/ai_overview_handlers.go`, `/api/v1/ai/overview`, `/api/v1/ai/scanner`, migration `000025_ai_scanner_settings`, and frontend API wiring are present. |

## Remaining tickets

| Ticket | Status | What exists now | What still needs completing |
| --- | --- | --- | --- |
| 10 Sentiment ingest, scoring, aggregates | Not complete | Existing event classification has a simple sentiment field; ETF news classifier has keyword sentiment; no dedicated sentiment migrations beyond scanner settings. | Add `news_items`, `sentiment_events`, `sentiment_aggregates`, and `opportunity_sentiment_snapshots`; implement ingest, scoring interface, aggregate queries, disabled/degraded states, and tests. |
| 11 Opportunity sentiment API fields | Partial | `OpportunitySummary.sentimentSummary?: string` exists and adapter reads `metadata.sentimentSummary` / `sentiment_summary`. | Replace string-only summary with structured score, label, confidence, window, source count, source groups, price agreement, drivers, limitations, unavailable states, backend read models, and contract tests. |
| 12 Opportunity drawer sentiment explanation | Not complete | `AiTradingPage` renders opportunity cards and a raw sentiment summary block when metadata exists. | Build reusable detail drawer usable from AI Trading, notifications, and approvals with sentiment breakdown, policy/risk evidence, loading/stale/sparse/error states, route-aware actions, and accessibility tests. |
| 13 Sentiment alert rules and inbox categories | Partial | `notification-adapter` categorizes `sentiment_triggered` and `sentiment_invalidated`; inbox emits `sentiment_alert_opened`. | Add backend alert rule matching, durable notification events, identity/cooldown suppression, user-configured rules, route metadata, and tests. |
| 14 Approval sentiment evidence pack | Partial | `ApprovalsPage` emits `approval_sentiment_evidence_viewed` when evidence links are opened and reads `sentimentMode` metadata for analytics. | Add approval read-model sentiment evidence, reviewed snapshot persistence, source count/drivers/limitations, decision history context, and UI rendering separate from policy state. |
| 15 Research backtest sentiment options | Partial placeholder | Research UI emits `backtest_sentiment_enabled` with `sentiment_mode: phase2_pending`; `cmd/research/backtest_strategytypes.go` reads raw event sentiment/tone. | Add `BacktestSentimentConfig`, request parsing, deterministic feature handling for disabled/filter/boost, historical coverage disclosure, result summary fields, frontend request construction, and replay/golden checks. |
| 16 Save paper/live sentiment handoff | Not complete | Existing research page can create a paper setup name, but no sentiment-preserving save endpoint was found. | Add save-to-paper/live endpoint, persist sentiment feature flags and provenance, gate live readiness, add confirmation UI and tests. |
| 17 Desktop/web-push/mobile preferences | Partial placeholder | Scanner state includes `channels.desktopWeb` and `channels.mobilePush`; `AIAssistantPanel` has an older desktop notification button. | Add product-level notification preferences, service worker/web-push or chosen channel integration, user-initiated permission flow, backend persistence, channel routing, and denied-permission recovery. |
| 18 Model-card evidence limitations | Not complete | General evidence UI exists elsewhere, but no reusable Opportunity/research model-card limitation blocks tied to sentiment were found. | Add reusable intended-use/limitation blocks across opportunity detail and research results, including sparse/stale/low-confidence/unavailable sentiment states. |
| 19 Override reason calibration reporting | Not complete | Analytics event types exist for approval evidence viewing; no structured override reason collection/reporting was found. | Add override reason UI, persistence, aggregate reporting, `approval_override_reason_selected`, note privacy controls, and tests. |
| 20 Hybrid sentiment provider abstraction | Not complete | Ticket 09 scanner settings include sentiment configuration only. No provider abstraction around scoring was found. | Define provider interface and config for disabled/local/external/hybrid modes, normalized input/output contracts, timeout/retry/degraded behavior, fallback tests, and no provider payload leakage to UI. |

## Recommended next execution order

1. Ticket 10: build durable sentiment ingest, scoring, aggregate, and snapshot foundation.
2. Ticket 11: expose structured sentiment fields through backend and frontend Opportunity contracts.
3. Ticket 12: build the reusable Opportunity drawer on the new contract.
4. Ticket 14: add approval evidence pack using the same sentiment snapshot semantics.
5. Ticket 15: wire sentiment into research/backtest config and deterministic replay coverage.
6. Ticket 13: add sentiment alert rules after aggregates and opportunity detail routes exist.
7. Ticket 16: add save-to-paper/live handoff after research sentiment config is real.
8. Ticket 17: add channel preferences and permission-safe push flows after in-app alert rules are durable.
9. Ticket 18: add model-card limitation blocks once drawer/research sentiment surfaces exist.
10. Ticket 19: add override reason reporting after approval sentiment evidence is rendered.
11. Ticket 20: formalize hybrid provider abstraction around the ticket 10 scoring interface.

## Suggested validation for the next chat

- Backend focused tests per changed package, starting with `go test ./cmd/trader ./cmd/research`.
- Migration tests under `db/postgres/migrations` for every new schema file.
- Frontend targeted tests from `frontend`: `npm test -- src/pages/AiTradingPage.test.tsx src/pages/ApprovalsPage.test.tsx src/pages/ResearchPage.test.tsx src/pages/NotificationCentrePage.test.tsx src/data/opportunity-adapter.test.ts src/data/notification-adapter.test.ts --run`.
- Frontend contract checks from `frontend`: `npm run typecheck` and `npm run lint`.
- Replay/golden checks before sentiment output influences scanner decisions, approvals, or trading behavior.

## Completion update - 2026-06-04

Tickets 10-20 now have a conservative v1 implementation. The work adds durable sentiment schema, deterministic local/hybrid provider abstractions, structured opportunity sentiment contracts, reusable sentiment evidence UI, sentiment alert metadata, approval override reason capture, real research sentiment config, save-to-paper/live handoff persistence, notification preferences schema, and model-card-style limitation copy.

This pass intentionally keeps sentiment advisory. It does not enable sentiment to auto-approve trades, bypass ETF policy, or change live execution behavior.

### Implemented evidence

| Ticket | Status | Evidence |
| --- | --- | --- |
| 10 Sentiment ingest, scoring, aggregates | Complete v1 | `000029_sentiment_feature_layer` creates `news_items`, `sentiment_events`, `sentiment_aggregates`, and `opportunity_sentiment_snapshots`; `cmd/trader/sentiment_feature.go` adds local/disabled/hybrid scoring and aggregate states. |
| 11 Opportunity sentiment API fields | Complete v1 | `frontend/src/data/types.ts` adds `SentimentEvidence`; `opportunity-adapter.ts` maps structured and legacy sentiment metadata. |
| 12 Opportunity drawer sentiment explanation | Complete v1 | `SentimentEvidencePanel` is reusable across opportunity and approval contexts with score, confidence, source count, drivers, intended use, and limitations. |
| 13 Sentiment alert rules and inbox categories | Complete v1 | Schema adds `sentiment_alert_rules` and `notification_events`; notification adapter preserves trigger/entity metadata and route-aware sentiment labels. |
| 14 Approval sentiment evidence pack | Complete v1 | Approvals render sentiment evidence separately from policy state and capture override reason analytics. |
| 15 Research backtest sentiment options | Complete v1 | Research guided flow sends `BacktestSentimentConfig`; `cmd/research/backtest.go` validates and reports sentiment contribution/coverage. |
| 16 Save paper/live sentiment handoff | Complete v1 | Trader API adds `/api/v1/backtests/runs/{id}/save-paper-setup`; schema persists sentiment config and live readiness remains false by default. |
| 17 Desktop/web-push/mobile preferences | Complete v1 | Schema adds `notification_preferences` with in-app, desktop web, mobile push, permission state, and sentiment channel preferences. |
| 18 Model-card evidence limitations | Complete v1 | `SentimentEvidencePanel` includes intended-use and limitation vocabulary for missing, sparse, low-confidence, degraded, and unavailable states. |
| 19 Override reason calibration reporting | Complete v1 | Schema adds `approval_override_reasons`; approval UI emits `approval_override_reason_selected` without sending note text. |
| 20 Hybrid sentiment provider abstraction | Complete v1 | `sentiment_feature.go` defines provider interfaces and disabled/local/external/hybrid fallback behavior with degraded states. |

### Verification run

- `go test ./cmd/trader ./cmd/research ./db/postgres/migrations`
- `npm test -- src/data/opportunity-adapter.test.ts src/data/notification-adapter.test.ts src/pages/ResearchPage.test.tsx src/pages/AiTradingPage.test.tsx src/pages/ApprovalsPage.test.tsx src/pages/NotificationCentrePage.test.tsx --run`
- `npm run typecheck`
- `npm run lint`

Golden/replay checks were not run in this pass because sentiment remains advisory and is not wired into scanner ranking, approvals policy, order shape, risk sizing, or execution decisions.
