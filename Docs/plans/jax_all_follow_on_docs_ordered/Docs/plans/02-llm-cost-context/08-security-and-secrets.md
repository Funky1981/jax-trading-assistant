# 08 — Security and Secrets

Rules:

```text
Jax uses LiteLLM virtual keys
LiteLLM stores provider keys
LiteLLM private network only
No provider keys in Git
No secrets in prompt logs
No secrets in evidence bundles
```

Use separate keys:

```text
jax-research-dev
jax-paper-trading
codex-personal
n8n-digest
manual-testing
```
