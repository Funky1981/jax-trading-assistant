# 04 — Token Tracking and Budgets

Track:

```text
task_type
model_alias
provider_model
input_tokens
output_tokens
total_tokens
estimated_cost
actual_cost
cache_hit
virtual_key
event_id
candidate_id
strategy_id
symbol
correlation_id
created_at
```

Metrics:

```text
cost per candidate
cost per approved paper trade
cost per rejected event
cache hit rate
paid calls avoided
most expensive task types
provider/model spend
```

Budgets:

```text
per call
per event
per candidate
per strategy
per day
per month
per virtual key
```
