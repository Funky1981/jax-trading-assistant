# 06 — Cost-Saving Patterns

n8n reduces cost by avoiding calls:

```text
dedupe before AI
rule-based pre-filter
priced-in before AI
batch summaries
local model first
paid escalation only
cache safe outputs
alert only on change
```

Track:

```text
model calls avoided
paid calls avoided
cache hit rate
cost per candidate
events rejected before AI
duplicate event rate
```
