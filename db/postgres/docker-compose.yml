services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: jax
      POSTGRES_USER: jax
      POSTGRES_PASSWORD: jax
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jax -d jax"]
      interval: 2s
      timeout: 3s
      retries: 20

